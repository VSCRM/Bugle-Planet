/**
 * Re-exports `useAuth` from authContext so components can import it via
 * `../../hooks/useAuth` rather than the longer `../../context/authContext`.
 */
export {useAuth} from "../context/authContext";
