# ─── Data Source ──────────────────────────────────────
# true  → static mock data (no backend required)
# false → real REST API via axios
VITE_USE_MOCK=true

# ─── API (active when VITE_USE_MOCK=false) ────────────
VITE_API_URL=http://localhost:3001/api

# ─── Mock delay (ms) ──────────────────────────────────
VITE_MOCK_DELAY_MS=600

# ─── EmailJS (password reset emails) ─────────────────
# Leave blank to show the reset code in the UI (dev mode)
# Get your keys at https://www.emailjs.com
VITE_EMAILJS_SERVICE_ID=service_uem5che
VITE_EMAILJS_TEMPLATE_ID=template_ms7fcoa
VITE_EMAILJS_PUBLIC_KEY=Shbdrej0_7C6_qYUW
